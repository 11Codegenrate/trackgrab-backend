services:
  - type: web
    name: trackgrab-backend
    env: node
    buildCommand: bash build.sh
    startCommand: node server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
